// Filled by the catalog-ops management task (Task 5 of the native management plan).
import type { ExtensionServices } from "../services";

export function registerCatalogOps(_services: ExtensionServices): void {}
